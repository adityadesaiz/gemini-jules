generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model AdaptiveMemory {
  id                String    @id @default(uuid())
  fieldLabelSemantic String
  userInput         String
  createdAt         DateTime  @default(now())
}
